version: "3.9"

services:
  api:
    build: .
    container_name: url-shortener-api
    env_file:
      - .env
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    restart: unless-stopped

